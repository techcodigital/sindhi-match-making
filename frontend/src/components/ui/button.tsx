import { cn } from '@/utils/cn'; import type { ButtonHTMLAttributes } from 'react';
export function Button({ className, variant='primary', ...props }: ButtonHTMLAttributes<HTMLButtonElement> & { variant?:'primary'|'secondary'|'ghost' }) { const variants = { primary:'bg-green-600 text-white hover:bg-green-700 shadow-lg shadow-green-200', secondary:'bg-green-50 text-green-700 hover:bg-green-100', ghost:'bg-transparent text-slate-600 hover:bg-slate-100' }; return <button className={cn('inline-flex min-h-11 items-center justify-center gap-2 rounded-xl px-4 text-sm font-bold transition active:scale-[.98] disabled:opacity-50', variants[variant], className)} {...props} />; }
